'use client';

import Link from 'next/link';
import { usePathname, useSearchParams } from 'next/navigation';
import { useCallback, useEffect, useMemo, useRef, useState, type ReactNode } from 'react';
import { useTranslations } from 'next-intl';
import { ArrowLeft, Beaker, Copy, Droplets, Flame, Link2, MessageCircle, Move, RotateCcw, RotateCw, Send, Snowflake, Sparkles, Trash2, Wind, X, ZoomIn, ZoomOut } from 'lucide-react';
import ThemeToggle from '@/components/common/ThemeToggle';
import LanguageSwitcher from '@/components/common/LanguageSwitcher';
import EquipmentIcon, { type EquipmentOperation } from '@/components/sandbox/equipment/EquipmentIcon';
import OnboardingHint from '@/components/common/OnboardingHint';

const ru = (locale: string) => locale === 'ru';
const uz = (locale: string) => locale === 'uz';
type EquipmentType = 'beaker' | 'erlenmeyer' | 'roundflask' | 'testtube' | 'burner' | 'hotplate' | 'thermometer' | 'condenser' | 'icebath' | 'phmeter' | 'scales';
type MaterialState = 'liquid' | 'gas' | 'solid';
type Material = { id: string; name: string; formula: string; color: string; state: MaterialState };
type Item = { id: string; type: EquipmentType; name: string; x: number; y: number; w: number; h: number; scale: number; rotation: number; material?: Material; volumeMl: number; liquidLevel: number; temperature: number; targetTemperature?: number; operation: EquipmentOperation; attachedTo?: string; broken?: boolean };
type Port = 'Glass' | 'Liquid' | 'Gas' | 'Thermal' | 'Electrical';
type Connection = { id: string; from: string; to: string; port: Port; direction: 'source-to-target' | 'target-to-source' };
type LibraryItem = { id: EquipmentType; name: string; w: number; h: number; icon: typeof Beaker };

const liquids: Material[] = [
  { id: 'water', name: 'Water', formula: 'H₂O', color: '#22D3EE', state: 'liquid' },
  { id: 'ethanol', name: 'Ethanol', formula: 'C₂H₅OH', color: '#F59E0B', state: 'liquid' },
  { id: 'acid', name: 'Hydrochloric acid', formula: 'HCl', color: '#F43F5E', state: 'liquid' },
  { id: 'oxygen', name: 'Oxygen', formula: 'O₂', color: '#A78BFA', state: 'gas' },
];
const compounds: Material[] = [
  { id: 'nacl', name: 'Sodium chloride', formula: 'NaCl', color: '#CBD5E1', state: 'solid' },
  { id: 'naoh', name: 'Sodium hydroxide', formula: 'NaOH', color: '#F8FAFC', state: 'solid' },
  { id: 'cuso4', name: 'Copper sulfate crystals', formula: 'CuSO₄·5H₂O', color: '#2563EB', state: 'solid' },
];
const elements: Material[] = [
  { id: 'copper', name: 'Copper', formula: 'Cu', color: '#B87333', state: 'solid' },
  { id: 'iron', name: 'Iron', formula: 'Fe', color: '#64748B', state: 'solid' },
  { id: 'sulfur', name: 'Sulfur', formula: 'S', color: '#FACC15', state: 'solid' },
];
const groups: { title: string; items: LibraryItem[] }[] = [
  { title: 'Containers', items: [{ id: 'beaker', name: 'Beaker', w: 100, h: 100, icon: Beaker }, { id: 'erlenmeyer', name: 'Erlenmeyer Flask', w: 100, h: 105, icon: Beaker }, { id: 'roundflask', name: 'Round-bottom Flask', w: 100, h: 110, icon: Beaker }, { id: 'testtube', name: 'Test Tube', w: 72, h: 110, icon: Beaker }] },
  { title: 'Heating', items: [{ id: 'burner', name: 'Bunsen Burner', w: 56, h: 82, icon: Flame }, { id: 'hotplate', name: 'Hot Plate', w: 140, h: 75, icon: Flame }] },
  { title: 'Cooling & Sensors', items: [{ id: 'icebath', name: 'Ice Bath', w: 120, h: 80, icon: Snowflake }, { id: 'thermometer', name: 'Thermometer', w: 70, h: 110, icon: Beaker }, { id: 'phmeter', name: 'pH Meter', w: 80, h: 105, icon: Beaker }, { id: 'scales', name: 'Precision Scale', w: 100, h: 80, icon: Beaker }, { id: 'condenser', name: 'Condenser', w: 220, h: 100, icon: Wind }] },
];
const isVessel = (item: Item) => ['beaker', 'erlenmeyer', 'roundflask', 'testtube'].includes(item.type);

export default function SandboxPage() {
  const pathname = usePathname();
  const query = useSearchParams();
  const locale = pathname.split('/')[1] || 'en';
  const ts = useTranslations('sandbox');
  const workspaceId = query.get('workspace');
  const template = query.get('template');
  const canvasRef = useRef<HTMLDivElement>(null);
  const [items, setItems] = useState<Item[]>([]);
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const [libraryTab, setLibraryTab] = useState<'equipment' | 'liquids' | 'compounds' | 'elements'>('equipment');
  const [tool, setTool] = useState<'select' | 'pan' | 'connect'>('select');
  const [zoom, setZoom] = useState(1);
  const [pan, setPan] = useState({ x: 0, y: 0 });
  const [connections, setConnections] = useState<Connection[]>([]);
  const [connectSource, setConnectSource] = useState<string | null>(null);
  const [connectionDraft, setConnectionDraft] = useState<{ from: string; to: string } | null>(null);
  const [pourSource, setPourSource] = useState<string | null>(null);
  const [pourAmount, setPourAmount] = useState(25);
  const [notice, setNotice] = useState<string | null>(null);
  const [pourAnimation, setPourAnimation] = useState<string | null>(null);
  const [chatOpen, setChatOpen] = useState(false);
  const [chatInput, setChatInput] = useState('');
  const [chat, setChat] = useState<Array<{ role: 'user' | 'assistant'; text: string }>>([{ role: 'assistant', text: locale === 'ru' ? 'Добавьте колбу и горелку. Я помогу безопасно настроить эксперимент.' : 'Add a flask and burner. I can help configure the experiment safely.' }]);
  const dragRef = useRef<{ id: string; dx: number; dy: number } | null>(null);
  const panRef = useRef<{ x: number; y: number; startX: number; startY: number } | null>(null);
  const pointerRef = useRef({ x: 0, y: 0 });
  const templateLoaded = useRef(false);
  const selected = items.find((item) => item.id === selectedId);
  useEffect(() => {
    const dialog = document.querySelector<HTMLElement>('[role="dialog"]');
    if (!dialog) return;
    const previous = document.activeElement instanceof HTMLElement ? document.activeElement : null;
    const focusable = () => Array.from(dialog.querySelectorAll<HTMLElement>('button, select, input, [tabindex]:not([tabindex="-1"])')).filter((element) => !element.hasAttribute('disabled'));
    focusable()[0]?.focus();
    const onKeyDown = (event: KeyboardEvent) => {
      if (event.key === 'Escape') {
        setConnectionDraft(null);
        setPourSource(null);
        return;
      }
      if (event.key !== 'Tab') return;
      const elements = focusable();
      if (!elements.length) return;
      const first = elements[0];
      const last = elements[elements.length - 1];
      if (event.shiftKey && document.activeElement === first) { event.preventDefault(); last.focus(); }
      else if (!event.shiftKey && document.activeElement === last) { event.preventDefault(); first.focus(); }
    };
    document.addEventListener('keydown', onKeyDown);
    return () => { document.removeEventListener('keydown', onKeyDown); previous?.focus(); };
  }, [connectionDraft, pourSource]);
  useEffect(() => {
    const l = {
      experiment: ts('untitled'), equipment: ts('equipment'), materials: ts('materials'), compounds: ts('cat_compounds'),
      elements: ts('cat_elements'), heating: ts('groupHeating'), cooling: ts('groupMeasurement'), inspector: ts('properties'),
      details: ts('properties'), select: ts('selectProperties'), ports: ts('connectionMode'), volume: ts('volume'),
      temperature: ts('temperature'), objects: ts('itemsCount', { count: items.length }), log: ts('logTab'), add: ts('addedToCanvas', { name: '' }),
      attached: ts('connectionCreated'), configure: ts('connectionMode'), type: ts('type'), direction: ts('position'), cancel: ts('cancel'),
      connect: ts('connectionCreated'), pour: ts('addedToContainer', { name: '' }), ask: ts('connectionMode'),
    };
    const map: Record<string, string> = { 'Workspace experiment': l.experiment, 'Untitled Experiment': l.experiment, Equipment: l.equipment, Materials: l.materials, Compounds: l.compounds, Elements: l.elements, Heating: l.heating, 'Cooling & Sensors': l.cooling, Containers: ru(locale) ? 'Сосуды' : uz(locale) ? 'Idishlar' : 'Containers', Inspector: l.inspector, 'Equipment details': l.details, 'Select equipment to see size, temperature, ports and operations.': l.select, 'Ports & connectors': l.ports, 'Experiment log': l.log, Objects: l.objects, Volume: l.volume, Temperature: l.temperature, Add: l.add, 'Attached · Heat link': l.attached, 'Configure connection': l.configure, Type: l.type, Direction: l.direction, Cancel: l.cancel, Connect: l.connect, 'Pour into another vessel': l.pour, 'Ask assistant…': l.ask, 'Bunsen Burner': ru(locale) ? 'Горелка Бунзена' : uz(locale) ? 'Bunsen gorelkasi' : 'Bunsen Burner', 'Hot Plate': ru(locale) ? 'Нагревательная плитка' : uz(locale) ? 'Qizdirish plitasi' : 'Hot Plate', 'Beaker': ru(locale) ? 'Стакан' : uz(locale) ? 'Stakan' : 'Beaker', 'Erlenmeyer Flask': ru(locale) ? 'Колба Эрленмейера' : uz(locale) ? 'Erlenmeyer kolbasi' : 'Erlenmeyer Flask', 'Round-bottom Flask': ru(locale) ? 'Круглодонная колба' : uz(locale) ? 'Dumaloq tubli kolba' : 'Round-bottom Flask', 'Test Tube': ru(locale) ? 'Пробирка' : uz(locale) ? 'Probirkа' : 'Test Tube', 'Ice Bath': ru(locale) ? 'Ледяная баня' : uz(locale) ? 'Muz vannasi' : 'Ice Bath', Thermometer: ru(locale) ? 'Термометр' : uz(locale) ? 'Termometr' : 'Thermometer', 'pH Meter': ru(locale) ? 'pH-метр' : uz(locale) ? 'pH o‘lchagich' : 'pH Meter', 'Precision Scale': ru(locale) ? 'Точные весы' : uz(locale) ? 'Aniq tarozi' : 'Precision Scale', Condenser: ru(locale) ? 'Конденсор' : uz(locale) ? 'Kondensator' : 'Condenser', 'AI Lab Assistant': ru(locale) ? 'AI-помощник лаборатории' : uz(locale) ? 'AI laboratoriya yordamchisi' : 'AI Lab Assistant', 'Safety-aware help': ru(locale) ? 'Помощь с учётом безопасности' : uz(locale) ? 'Xavfsizlikka mos yordam' : 'Safety-aware help' };
    if (ru(locale)) Object.assign(map, { Equipment: 'Оборудование', Materials: 'Материалы', Compounds: 'Соединения', Elements: 'Элементы', Heating: 'Нагрев', 'Cooling & Sensors': 'Охлаждение и датчики', Containers: 'Сосуды', Inspector: 'Инспектор', 'Equipment details': 'Детали оборудования', 'Selected equipment': 'Выбранное оборудование', Size: 'Размер', Heat: 'Нагрев', Cool: 'Охлаждение', Stir: 'Перемешивание', Objects: 'Объекты', Volume: 'Объём', Temperature: 'Температура', Add: 'Добавить', Connect: 'Соединить', Cancel: 'Отмена', Type: 'Тип', Direction: 'Направление', 'Source → target': 'От источника к цели', 'Configure connection': 'Настроить соединение', 'Bunsen Burner': 'Горелка Бунзена', 'Hot Plate': 'Нагревательная плитка', Beaker: 'Стакан', 'Erlenmeyer Flask': 'Колба Эрленмейера', 'Round-bottom Flask': 'Круглодонная колба', 'Test Tube': 'Пробирка', 'Ice Bath': 'Ледяная баня', Thermometer: 'Термометр', Condenser: 'Конденсор', 'AI Lab Assistant': 'AI-помощник лаборатории' });
    if (uz(locale)) Object.assign(map, { Equipment: 'Uskunalar', Materials: 'Materiallar', Compounds: 'Biriktmalar', Elements: 'Elementlar', Heating: 'Qizdirish', 'Cooling & Sensors': 'Sovitish va sensorlar', Containers: 'Idishlar', Inspector: 'Inspektor', 'Equipment details': 'Uskuna tafsilotlari', 'Selected equipment': 'Tanlangan uskuna', Size: 'O‘lcham', Heat: 'Qizdirish', Cool: 'Sovitish', Stir: 'Aralashtirish', Type: 'Tur', Direction: 'Yo‘nalish', 'Source → target': 'Manbadan nishonga', Cancel: 'Bekor qilish', Connect: 'Ulash', 'Configure connection': 'Ulanishni sozlash', 'Hot Plate': 'Qizdirish plitasi' });
    const walker = document.createTreeWalker(document.body, NodeFilter.SHOW_TEXT);
    const nodes: Text[] = []; let node: Node | null; while ((node = walker.nextNode())) nodes.push(node as Text);
    nodes.forEach((node) => { const value = node.textContent?.trim(); if (value && map[value]) node.textContent = node.textContent?.replace(value, map[value]) || map[value]; });
  }, [connectionDraft, items.length, locale, pourSource, selectedId, ts]);
  useEffect(() => {
    if (template !== 'acid-base' || templateLoaded.current) return;
    templateLoaded.current = true;
    const timer = window.setTimeout(() => {
      const flask = { id: 'template-flask', type: 'erlenmeyer' as EquipmentType, name: 'Erlenmeyer Flask', x: 300, y: 220, w: 100, h: 105, scale: 1, rotation: 0, volumeMl: 50, liquidLevel: .5, material: liquids[0], temperature: 24.5, operation: 'idle' as EquipmentOperation };
      const burner = { id: 'template-burner', type: 'burner' as EquipmentType, name: 'Bunsen Burner', x: 322, y: 314, w: 56, h: 82, scale: 1, rotation: 0, volumeMl: 0, liquidLevel: 0, temperature: 24.5, operation: 'idle' as EquipmentOperation, attachedTo: flask.id };
      const thermometer = { id: 'template-thermometer', type: 'thermometer' as EquipmentType, name: 'Thermometer', x: 440, y: 215, w: 70, h: 110, scale: 1, rotation: 0, volumeMl: 0, liquidLevel: 0, temperature: 24.5, operation: 'idle' as EquipmentOperation };
      setItems([flask, burner, thermometer]);
      setConnections([{ id: 'template-thermal', from: burner.id, to: flask.id, port: 'Thermal', direction: 'source-to-target' }, { id: 'template-glass', from: thermometer.id, to: flask.id, port: 'Glass', direction: 'source-to-target' }]);
      setSelectedId(flask.id);
      setNotice('Template loaded: connected flask, burner and thermometer');
    }, 0);
    return () => window.clearTimeout(timer);
  }, [template]);
  const centers = useMemo(() => new Map(items.map((item) => [item.id, { x: item.x + item.w * item.scale / 2, y: item.y + item.h * item.scale / 2 }])), [items]);

  const addItem = useCallback((libraryItem: LibraryItem) => {
    const bounds = canvasRef.current?.getBoundingClientRect();
    const index = items.length;
    const next: Item = { id: `${libraryItem.id}-${crypto.randomUUID()}`, type: libraryItem.id, name: libraryItem.name, x: Math.max(24, (bounds?.width || 800) / 2 - libraryItem.w / 2 + index * 18), y: Math.max(90, (bounds?.height || 600) / 2 - libraryItem.h / 2), w: libraryItem.w, h: libraryItem.h, scale: 1, rotation: 0, volumeMl: 0, liquidLevel: 0, temperature: 24.5, operation: 'idle' };
    setItems((current) => [...current, next]); setSelectedId(next.id);
  }, [items.length]);

  const updateItem = (id: string, patch: Partial<Item>) => setItems((current) => current.map((item) => item.id === id ? { ...item, ...patch } : item));
  const addMaterial = (material: Material) => {
    if (!selected || !isVessel(selected)) { setNotice('Select a vessel first'); return; }
    if (material.state === 'gas') { setNotice('Oxygen is a gas: an open flask cannot retain it. Use a sealed Gas connector.'); return; }
    updateItem(selected.id, { material, volumeMl: material.state === 'liquid' ? Math.max(25, selected.volumeMl) : selected.volumeMl, liquidLevel: material.state === 'liquid' ? Math.max(.35, selected.liquidLevel) : selected.liquidLevel });
    setPourAnimation(selected.id); window.setTimeout(() => setPourAnimation(null), 900); setNotice(`${material.name} added · ${material.state === 'liquid' ? '25 mL' : 'solid sample'}`);
  };
  const applyOperation = (item: Item, operation: EquipmentOperation) => {
    const defaultTarget = item.targetTemperature || (operation === 'heating' ? 80 : operation === 'cooling' ? 5 : item.temperature);
    const requestedTarget = operation === 'heating' ? Number(window.prompt('Target temperature (°C)', String(defaultTarget)) || defaultTarget) : defaultTarget;
    const target = Number.isFinite(requestedTarget) ? Math.max(0, requestedTarget) : defaultTarget;
    setItems((current) => current.map((value) => value.id === item.id || (item.type === 'burner' && item.attachedTo === value.id) || (value.type === 'burner' && value.attachedTo === item.id) ? { ...value, operation, targetTemperature: operation === 'idle' || operation === 'stirring' ? value.targetTemperature : target } : value));
    if (operation === 'heating' && item.type === 'burner' && !item.attachedTo) setNotice(ru(locale) ? 'Сначала приблизьте горелку к сосуду.' : uz(locale) ? 'Avval gorelkani idishga yaqinlashtiring.' : 'Move the burner close to a vessel first.');
  };
  useEffect(() => {
    const timer = window.setInterval(() => {
      setItems((current) => current.map((item) => {
        if (!isVessel(item) || item.broken || item.operation === 'idle' || item.operation === 'stirring') return item;
        const delta = item.operation === 'heating' ? 2 : item.operation === 'cooling' ? -2 : 0;
        const nextTemperature = Math.max(0, item.temperature + delta);
        const target = item.targetTemperature || 80;
        if (item.operation === 'heating' && nextTemperature >= 180) {
          setNotice(ru(locale) ? `${item.name}: сосуд разрушен из-за перегрева.` : uz(locale) ? `${item.name}: idish qizib yorildi.` : `${item.name}: vessel broke from overheating.`);
          return { ...item, temperature: nextTemperature, operation: 'idle', broken: true };
        }
        if (item.operation === 'heating' && nextTemperature >= target) return { ...item, temperature: target, operation: 'idle' };
        if (item.operation === 'cooling' && nextTemperature <= target) return { ...item, temperature: target, operation: 'idle' };
        return { ...item, temperature: nextTemperature };
      }));
    }, 500);
    return () => window.clearInterval(timer);
  }, [locale]);
  const endDrag = () => {
    const drag = dragRef.current; if (!drag) return;
    const burner = items.find((item) => item.id === drag.id);
    if (burner?.type === 'burner') {
      const nearest = items.filter(isVessel).map((item) => ({ item, distance: Math.hypot(item.x - burner.x, item.y - burner.y) })).sort((a, b) => a.distance - b.distance)[0];
      if (nearest && nearest.distance < 130) { updateItem(burner.id, { x: nearest.item.x + nearest.item.w / 2 - burner.w / 2, y: nearest.item.y + nearest.item.h - burner.h * .1, attachedTo: nearest.item.id }); setNotice(`Burner attached to ${nearest.item.name}`); }
    }
    dragRef.current = null;
  };
  const onPointerDown = (event: React.PointerEvent<HTMLDivElement>, id: string) => {
    event.stopPropagation(); setSelectedId(id);
    if (tool === 'connect') { if (!connectSource) setConnectSource(id); else if (connectSource !== id) { setConnectionDraft({ from: connectSource, to: id }); setConnectSource(null); setTool('select'); } return; }
    if (tool !== 'select') return;
    const item = items.find((value) => value.id === id); const bounds = canvasRef.current?.getBoundingClientRect(); if (!item || !bounds) return;
    event.currentTarget.setPointerCapture(event.pointerId); dragRef.current = { id, dx: (event.clientX - bounds.left) / zoom - item.x, dy: (event.clientY - bounds.top) / zoom - item.y };
  };
  const onPointerMove = (event: React.PointerEvent<HTMLDivElement>) => { pointerRef.current = { x: event.clientX, y: event.clientY }; const bounds = canvasRef.current?.getBoundingClientRect(); if (tool === 'pan' && panRef.current) { setPan({ x: panRef.current.x + event.clientX - panRef.current.startX, y: panRef.current.y + event.clientY - panRef.current.startY }); return; } const drag = dragRef.current; if (!drag || !bounds) return; setItems((current) => { const nextX = Math.max(0, (event.clientX - bounds.left) / zoom - drag.dx); const nextY = Math.max(70, (event.clientY - bounds.top) / zoom - drag.dy); const moved = current.map((item) => item.id === drag.id ? { ...item, x: nextX, y: nextY } : item); const vessel = moved.find((item) => item.id === drag.id); return vessel && isVessel(vessel) ? moved.map((item) => item.type === 'burner' && item.attachedTo === vessel.id ? { ...item, x: vessel.x + vessel.w / 2 - item.w / 2, y: vessel.y + vessel.h - item.h * .1 } : item) : moved; }); };
  const pour = (targetId: string) => { const source = items.find((item) => item.id === pourSource); const target = items.find((item) => item.id === targetId); if (!source?.material || source.material.state !== 'liquid' || !target || !isVessel(target)) { setNotice('Only liquids can be poured between open vessels.'); return; } const amount = Math.min(pourAmount, source.volumeMl); updateItem(source.id, { volumeMl: source.volumeMl - amount, liquidLevel: Math.max(0, source.liquidLevel - amount / 100) }); updateItem(target.id, { material: source.material, volumeMl: target.volumeMl + amount, liquidLevel: Math.min(1, target.liquidLevel + amount / 100) }); setPourAnimation(target.id); window.setTimeout(() => setPourAnimation(null), 900); setPourSource(null); setNotice(`Poured ${amount} mL into ${target.name}`); };
  const remove = () => { if (!selectedId) return; setItems((current) => current.filter((item) => item.id !== selectedId)); setConnections((current) => current.filter((link) => link.from !== selectedId && link.to !== selectedId)); setSelectedId(null); };
  const duplicate = () => { if (!selected) return; const copy = { ...selected, id: `${selected.type}-${crypto.randomUUID()}`, x: selected.x + 24, y: selected.y + 24 }; setItems((current) => [...current, copy]); setSelectedId(copy.id); };
  const sendChat = () => { if (!chatInput.trim()) return; const question = chatInput.trim(); setChat((current) => [...current, { role: 'user', text: question }, { role: 'assistant', text: locale === 'ru' ? 'Для нагрева приблизьте горелку к колбе до появления Attached, затем нажмите Heat. Газ помещайте только через герметичный Gas-порт.' : 'Move the burner close to the flask until Attached appears, then press Heat. Add gases only through a sealed Gas port.' }]); setChatInput(''); };
  useEffect(() => { const key = (event: KeyboardEvent) => { if (event.key === 'Delete') remove(); if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === 'd') { event.preventDefault(); duplicate(); } }; window.addEventListener('keydown', key); return () => window.removeEventListener('keydown', key); });
  useEffect(() => { const canvas = canvasRef.current; if (!canvas || tool !== 'pan') return; const down = (event: PointerEvent) => { panRef.current = { x: pan.x, y: pan.y, startX: event.clientX, startY: event.clientY }; canvas.setPointerCapture(event.pointerId); }; const up = () => { panRef.current = null; }; canvas.addEventListener('pointerdown', down); canvas.addEventListener('pointerup', up); canvas.addEventListener('pointercancel', up); return () => { canvas.removeEventListener('pointerdown', down); canvas.removeEventListener('pointerup', up); canvas.removeEventListener('pointercancel', up); }; }, [pan.x, pan.y, tool]);
  useEffect(() => { if (canvasRef.current) canvasRef.current.style.cursor = tool === 'connect' ? 'crosshair' : tool === 'pan' ? 'grab' : 'default'; }, [tool]);
  useEffect(() => { const canvas = canvasRef.current; if (!canvas) return; const nodes = Array.from(canvas.querySelectorAll<HTMLElement>(':scope > div[role="button"]')); nodes.forEach((node, index) => { const item = items[index]; if (!item) return; node.style.left = `${item.x * zoom + pan.x}px`; node.style.top = `${item.y * zoom + pan.y}px`; node.style.width = `${item.w * item.scale * zoom}px`; node.style.height = `${item.h * item.scale * zoom}px`; }); }, [items, pan, zoom]);

  return <div className="flex h-[100dvh] min-h-0 overflow-hidden bg-[var(--background)] text-[var(--foreground)]"><aside className="hidden w-[300px] shrink-0 flex-col border-r border-[var(--border)] bg-[var(--card)] xl:flex"><header className="flex items-center justify-between border-b border-[var(--border)] p-4"><Link href={`/${locale}/dashboard`} aria-label="Back to dashboard"><ArrowLeft /></Link><span className="truncate text-sm font-semibold">{workspaceId ? 'Workspace experiment' : 'Untitled Experiment'}</span><div className="flex gap-1"><ThemeToggle /><LanguageSwitcher /></div></header><Library tab={libraryTab} setTab={setLibraryTab} addItem={addItem} addMaterial={addMaterial} selected={selected} /></aside><main className="relative flex min-w-0 flex-1 flex-col"><header className="flex min-h-14 items-center justify-between border-b border-[var(--border)] bg-[var(--card)]/95 px-3 xl:hidden"><Link href={`/${locale}/dashboard`} aria-label="Back to dashboard"><ArrowLeft /></Link><span className="truncate text-sm font-semibold">{workspaceId ? 'Workspace experiment' : 'Untitled Experiment'}</span><div className="flex gap-1"><ThemeToggle /><LanguageSwitcher /></div></header><div className="absolute left-1/2 top-16 z-30 flex -translate-x-1/2 gap-1 rounded-xl border border-[var(--border)] bg-[var(--card)]/95 p-1 shadow-lg sm:top-4"><ToolButton label="Select" active={tool === 'select'} onClick={() => setTool('select')}>↖</ToolButton><ToolButton label="Pan" active={tool === 'pan'} onClick={() => setTool('pan')}><Move size={17} /></ToolButton><ToolButton label="Connect" active={tool === 'connect'} onClick={() => { setTool('connect'); setConnectSource(null); }}>⌁</ToolButton><span className="mx-1 h-6 w-px bg-[var(--border)]" /><ToolButton label="Zoom in" onClick={() => setZoom((value) => Math.min(2, value + .1))}><ZoomIn size={17} /></ToolButton><ToolButton label="Zoom out" onClick={() => setZoom((value) => Math.max(.5, value - .1))}><ZoomOut size={17} /></ToolButton></div><div ref={canvasRef} onPointerDown={() => { setSelectedId(null); setConnectSource(null); }} onPointerMove={onPointerMove} onPointerUp={endDrag} onPointerLeave={endDrag} className="relative flex-1 overflow-hidden" style={{ backgroundImage: 'radial-gradient(var(--border) 1px, transparent 1px)', backgroundSize: `${20 * zoom}px ${20 * zoom}px` }}><svg className="pointer-events-none absolute inset-0 h-full w-full">{connections.map((link) => { const from = centers.get(link.from); const to = centers.get(link.to); return from && to ? <line key={link.id} x1={from.x * zoom} y1={from.y * zoom} x2={to.x * zoom} y2={to.y * zoom} stroke="var(--primary)" strokeWidth="3" strokeDasharray="8 6" /> : null; })}</svg>{items.map((item) => <div key={item.id} role="button" tabIndex={0} aria-label={`${item.name}${item.material ? ` containing ${item.material.name}` : ''}`} onPointerDown={(event) => onPointerDown(event, item.id)} style={{ left: item.x, top: item.y, width: item.w * item.scale, height: item.h * item.scale, transform: `rotate(${item.rotation}deg)`, touchAction: 'none' }} className={`absolute rounded-xl p-1 outline-none focus:ring-2 focus:ring-[var(--primary)] ${selectedId === item.id ? 'z-20 ring-2 ring-[var(--primary)]' : 'z-10 hover:ring-1 hover:ring-[var(--primary)]/60'}`}><EquipmentIcon type={item.type} size={item.w * item.scale} liquidLevel={item.liquidLevel} liquidColor={item.material?.color} operation={item.operation} />{isVessel(item) && (selectedId === item.id || item.operation !== 'idle') && <span className="absolute bottom-1 left-1/2 -translate-x-1/2 rounded bg-[var(--card)]/90 px-1 text-[10px] font-semibold">{item.temperature.toFixed(1)}°C</span>}{item.attachedTo && <span className="absolute -bottom-5 left-1/2 -translate-x-1/2 whitespace-nowrap rounded bg-orange-500/15 px-1.5 py-0.5 text-[9px] font-semibold text-orange-600">Attached · Heat link</span>}{pourAnimation === item.id && <span className="sandbox-pour-stream absolute left-1/2 top-0 h-16 w-1 -translate-x-1/2 rounded-full bg-cyan-400" />}{selectedId === item.id && <SelectionToolbar duplicate={duplicate} remove={remove} connect={() => { setTool('connect'); setConnectSource(item.id); }} />}{(selectedId === item.id || tool === 'connect') && <div className="absolute -right-4 top-1/2 grid gap-1"><Port name="Glass" color="bg-slate-400" onClick={() => { setTool('connect'); setConnectSource(item.id); }} /><Port name="Liquid" color="bg-cyan-400" onClick={() => { setTool('connect'); setConnectSource(item.id); }} /><Port name="Gas" color="bg-violet-400" onClick={() => { setTool('connect'); setConnectSource(item.id); }} /></div>}</div>)}</div><ActivityLog items={items} selected={selected} /><div className="flex min-h-12 items-center justify-between border-t border-[var(--border)] bg-[var(--card)] px-3 text-xs"><span>Temperature {selected?.temperature.toFixed(1) || '24.5'} °C · Volume {selected?.volumeMl || 0} mL</span><button className="touch-target rounded-lg px-3 text-[var(--primary)]" onClick={() => setChatOpen((value) => !value)}><MessageCircle size={16} className="mr-1 inline" />AI assistant</button></div></main><aside className="hidden w-[310px] shrink-0 flex-col border-l border-[var(--border)] bg-[var(--card)] xl:flex"><div className="border-b border-[var(--border)] p-4"><p className="text-xs uppercase tracking-wider text-[var(--muted-foreground)]">Inspector</p><h2 className="mt-1 font-semibold">Equipment details</h2></div>{selected ? <Properties item={selected} update={updateItem} onOperation={applyOperation} setPourSource={setPourSource} pourSource={pourSource} /> : <p className="p-5 text-sm text-[var(--muted-foreground)]">Select equipment to see size, temperature, ports and operations.</p>}<div className="mt-auto border-t border-[var(--border)] p-4 text-xs text-[var(--muted-foreground)]"><p className="font-semibold text-[var(--foreground)]">Ports & connectors</p><div className="mt-2 grid grid-cols-2 gap-2"><span>● Glass</span><span className="text-cyan-500">● Liquid</span><span className="text-violet-500">● Gas</span><span className="text-orange-500">● Thermal</span><span className="text-emerald-500">● Electrical</span></div></div></aside>{connectionDraft && <ConnectionDialog onSave={(port, direction) => { setConnections((current) => [...current, { id: crypto.randomUUID(), from: connectionDraft.from, to: connectionDraft.to, port, direction }]); setConnectionDraft(null); setNotice(`Connected ${port}`); }} onClose={() => setConnectionDraft(null)} />}{pourSource && <PourDialog source={items.find((item) => item.id === pourSource)} targets={items.filter((item) => item.id !== pourSource && isVessel(item))} amount={pourAmount} setAmount={setPourAmount} onPour={pour} onClose={() => setPourSource(null)} />}{chatOpen && <AssistantPanel chat={chat} input={chatInput} setInput={setChatInput} onSend={sendChat} onClose={() => setChatOpen(false)} />}<button aria-label="Open AI assistant" onClick={() => setChatOpen((value) => !value)} className="fixed bottom-5 right-5 z-[70] grid h-14 w-14 place-items-center rounded-full bg-[var(--primary)] text-white shadow-xl"><MessageCircle /></button><OnboardingHint storageKey="sandbox-onboarding-v2" locale={locale} kind="sandbox" /></div>;
}

function ToolButton({ label, active, onClick, children }: { label: string; active?: boolean; onClick: () => void; children: ReactNode }) { return <button aria-label={label} title={label} onPointerDown={(event) => event.stopPropagation()} onClick={onClick} className={`min-h-11 min-w-11 rounded-lg p-2 ${active ? 'bg-[var(--primary)] text-white' : 'hover:bg-[var(--accent)]'}`}>{children}</button>; }
function Port({ name, color, onClick }: { name: string; color: string; onClick: () => void }) { return <button aria-label={`${name} port`} title={`${name} port — click to connect`} onPointerDown={(event) => event.stopPropagation()} onClick={(event) => { event.stopPropagation(); onClick(); }} className="group flex min-h-7 items-center gap-1"><span className={`h-5 w-5 shrink-0 rounded-full border-2 border-[var(--card)] shadow-sm ${color}`} /><span className="pointer-events-none absolute right-6 hidden whitespace-nowrap rounded bg-[var(--card)] px-1.5 py-0.5 text-[10px] font-semibold shadow-md group-hover:block">{name}</span></button>; }
function Library({ tab, setTab, addItem, addMaterial, selected }: { tab: 'equipment' | 'liquids' | 'compounds' | 'elements'; setTab: (tab: 'equipment' | 'liquids' | 'compounds' | 'elements') => void; addItem: (item: LibraryItem) => void; addMaterial: (material: Material) => void; selected?: Item }) { const list = tab === 'liquids' ? liquids : tab === 'compounds' ? compounds : elements; return <div className="flex min-h-0 flex-1 flex-col"><div className="grid grid-cols-4 gap-1 border-b border-[var(--border)] p-2"><button className={`min-h-11 rounded-lg text-[10px] font-semibold ${tab === 'equipment' ? 'bg-[var(--primary)] text-white' : 'hover:bg-[var(--accent)]'}`} onClick={() => setTab('equipment')}>Equipment</button><button className={`min-h-11 rounded-lg text-[10px] font-semibold ${tab === 'liquids' ? 'bg-[var(--primary)] text-white' : 'hover:bg-[var(--accent)]'}`} onClick={() => setTab('liquids')}>Materials</button><button className={`min-h-11 rounded-lg text-[10px] font-semibold ${tab === 'compounds' ? 'bg-[var(--primary)] text-white' : 'hover:bg-[var(--accent)]'}`} onClick={() => setTab('compounds')}>Compounds</button><button className={`min-h-11 rounded-lg text-[10px] font-semibold ${tab === 'elements' ? 'bg-[var(--primary)] text-white' : 'hover:bg-[var(--accent)]'}`} onClick={() => setTab('elements')}>Elements</button></div><div className="flex-1 overflow-y-auto p-4">{tab === 'equipment' ? groups.map((group) => <section key={group.title} className="mb-5"><h3 className="mb-3 text-xs font-bold uppercase tracking-wider text-[var(--muted-foreground)]">{group.title}</h3><div className="grid grid-cols-2 gap-3">{group.items.map((item) => <button key={item.id} className="min-h-24 rounded-xl border border-[var(--border)] p-3 text-center hover:border-[var(--primary)]" onClick={() => addItem(item)}><item.icon size={24} className="mx-auto mb-2 text-[var(--primary)]" /><span className="block text-xs font-medium">{item.name}</span><span className="mt-1 block text-[10px] font-bold uppercase text-[var(--primary)]">Add</span></button>)}</div></section>) : <div className="space-y-3">{list.map((material) => <button key={material.id} onClick={() => addMaterial(material)} className="flex min-h-14 w-full items-center gap-3 rounded-xl border border-[var(--border)] p-3 text-left hover:border-[var(--primary)]"><span className="h-9 w-9 rounded-full border-2 border-white/20" style={{ background: material.color }} /><span><span className="block text-sm font-semibold">{material.name}</span><span className="text-xs text-[var(--muted-foreground)]">{material.formula} · {material.state}</span></span></button>)}{!selected && <p className="mt-4 text-xs text-[var(--muted-foreground)]">Select a vessel before adding a sample.</p>}</div>}</div></div>; }
function SelectionToolbar({ duplicate, remove, connect }: { duplicate: () => void; remove: () => void; connect: () => void }) { return <div className="absolute -top-12 left-1/2 flex -translate-x-1/2 gap-1 rounded-lg border border-[var(--border)] bg-[var(--card)] p-1 shadow-lg"><ToolButton label="Duplicate" onClick={duplicate}><Copy size={15} /></ToolButton><ToolButton label="Delete" onClick={remove}><Trash2 size={15} /></ToolButton><ToolButton label="Connect" onClick={connect}><Link2 size={15} /></ToolButton></div>; }
function Properties({ item, update, onOperation, setPourSource, pourSource }: { item: Item; update: (id: string, patch: Partial<Item>) => void; onOperation: (item: Item, operation: EquipmentOperation) => void; setPourSource: (id: string | null) => void; pourSource: string | null }) { return <div className="space-y-5 overflow-y-auto p-5"><div><p className="text-xs uppercase tracking-wider text-[var(--muted-foreground)]">Selected equipment</p><h3 className="mt-1 text-lg font-bold">{item.name}</h3><p className="mt-1 text-sm font-semibold text-[var(--primary)]">{item.temperature.toFixed(1)} °C · {item.volumeMl} mL</p></div><label className="block text-sm">Size<input aria-label="Equipment size" type="range" min=".6" max="1.6" step=".05" value={item.scale} onChange={(event) => update(item.id, { scale: Number(event.target.value) })} className="mt-2 w-full" /></label><div className="flex gap-2"><button className="touch-target flex-1 rounded-xl border border-[var(--border)]" onClick={() => update(item.id, { rotation: item.rotation - 15 })} aria-label="Rotate counterclockwise"><RotateCcw className="mx-auto" size={17} /></button><button className="touch-target flex-1 rounded-xl border border-[var(--border)]" onClick={() => update(item.id, { rotation: item.rotation + 15 })} aria-label="Rotate clockwise"><RotateCw className="mx-auto" size={17} /></button></div><div className="grid grid-cols-3 gap-2"><ActionButton label="Heat" icon={<Flame size={15} />} onClick={() => onOperation(item, item.operation === 'heating' ? 'idle' : 'heating')} /><ActionButton label="Cool" icon={<Snowflake size={15} />} onClick={() => onOperation(item, item.operation === 'cooling' ? 'idle' : 'cooling')} /><ActionButton label="Stir" icon={<Sparkles size={15} />} onClick={() => onOperation(item, item.operation === 'stirring' ? 'idle' : 'stirring')} /></div>{item.material && <div className="rounded-xl border border-[var(--border)] p-3"><p className="font-semibold">{item.material.name} · {item.material.formula}</p><label className="mt-3 block text-xs text-[var(--muted-foreground)]">Volume (mL)<input aria-label="Volume in milliliters" type="number" min="0" max="1000" value={item.volumeMl} onChange={(event) => update(item.id, { volumeMl: Number(event.target.value), liquidLevel: Math.min(1, Number(event.target.value) / 100) })} className="mt-2 w-full rounded-lg border border-[var(--border)] bg-[var(--background)] p-2" /></label></div>}{isVessel(item) && item.material?.state === 'liquid' && <button className="min-h-11 w-full rounded-xl border border-[var(--primary)] text-sm font-semibold text-[var(--primary)]" onClick={() => setPourSource(pourSource ? null : item.id)}><Droplets size={15} className="mr-2 inline" />Pour into another vessel</button>}</div>; }
function ActionButton({ label, icon, onClick }: { label: string; icon: ReactNode; onClick: () => void }) { return <button className="flex min-h-11 items-center justify-center rounded-xl border border-[var(--border)] px-2 text-xs font-semibold hover:border-[var(--primary)]" onPointerDown={(event) => event.stopPropagation()} onClick={onClick}>{icon}<span className="ml-1">{label}</span></button>; }
function ActivityLog({ items, selected }: { items: Item[]; selected?: Item }) { return <section className="hidden min-h-[92px] border-t border-[var(--border)] bg-[var(--card)] px-4 py-3 lg:block"><h3 className="text-xs font-semibold uppercase tracking-wider">Experiment log</h3><div className="mt-3 grid grid-cols-4 gap-3 text-xs"><div><span className="text-[var(--muted-foreground)]">Objects</span><strong className="mt-1 block">{items.length}</strong></div><div><span className="text-[var(--muted-foreground)]">Volume</span><strong className="mt-1 block">{selected?.volumeMl || 0} mL</strong></div><div><span className="text-[var(--muted-foreground)]">Temperature</span><strong className="mt-1 block">{selected?.temperature.toFixed(1) || '24.5'} °C</strong></div><div><span className="text-[var(--muted-foreground)]">State</span><strong className="mt-1 block capitalize">{selected?.operation || 'idle'}</strong></div></div></section>; }
function ConnectionDialog({ onSave, onClose }: { onSave: (port: Port, direction: Connection['direction']) => void; onClose: () => void }) { const ts = useTranslations('sandbox'); const [port, setPort] = useState<Port>('Glass'); const [direction, setDirection] = useState<Connection['direction']>('source-to-target'); return <div className="fixed inset-0 z-[90] grid place-items-center bg-black/55 p-4" role="dialog" aria-modal="true" aria-labelledby="connection-dialog-title"><div className="w-full max-w-sm rounded-2xl border border-[var(--border)] bg-[var(--card)] p-5 shadow-2xl"><div className="flex items-center justify-between"><h2 id="connection-dialog-title" className="font-bold">{ts('configureConnection')}</h2><button className="touch-target" onClick={onClose} aria-label={ts('close')}><X size={18} /></button></div><label className="mt-4 block text-sm">{ts('type')}<select value={port} onChange={(event) => setPort(event.target.value as Port)} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] p-3"><option>Glass</option><option>Liquid</option><option>Gas</option><option>Thermal</option><option>Electrical</option></select></label><label className="mt-4 block text-sm">{ts('direction')}<select value={direction} onChange={(event) => setDirection(event.target.value as Connection['direction'])} className="mt-2 w-full rounded-xl border border-[var(--border)] bg-[var(--background)] p-3"><option value="source-to-target">{ts('sourceToTarget')}</option><option value="target-to-source">{ts('targetToSource')}</option></select></label><div className="mt-5 flex justify-end gap-2"><button className="min-h-11 rounded-xl border border-[var(--border)] px-4" onClick={onClose}>{ts('cancel')}</button><button className="min-h-11 rounded-xl bg-[var(--primary)] px-4 font-semibold text-white" onClick={() => onSave(port, direction)}>{ts('connect')}</button></div></div></div>; }
function PourDialog({ source, targets, amount, setAmount, onPour, onClose }: { source?: Item; targets: Item[]; amount: number; setAmount: (value: number) => void; onPour: (id: string) => void; onClose: () => void }) { const ts = useTranslations('sandbox'); return <div className="fixed inset-0 z-[90] grid place-items-center bg-black/55 p-4" role="dialog" aria-modal="true" aria-labelledby="pour-dialog-title"><div className="w-full max-w-sm rounded-2xl border border-[var(--border)] bg-[var(--card)] p-5"><div className="flex items-center justify-between"><h2 id="pour-dialog-title" className="font-bold">{ts('pourFrom', { name: source?.name ?? '' })}</h2><button className="touch-target" onClick={onClose} aria-label={ts('close')}><X size={18} /></button></div><label className="mt-4 block text-sm">{ts('amount')}: {amount} mL<input aria-label={ts('amount')} type="range" min="5" max="100" step="5" value={amount} onChange={(event) => setAmount(Number(event.target.value))} className="mt-2 w-full" /></label><div className="mt-4 grid gap-2">{targets.map((target) => <button key={target.id} className="min-h-11 rounded-xl border border-[var(--border)] text-left px-3" onClick={() => onPour(target.id)}>{target.name}</button>)}</div></div></div>; }
function AssistantPanel({ chat, input, setInput, onSend, onClose }: { chat: Array<{ role: 'user' | 'assistant'; text: string }>; input: string; setInput: (value: string) => void; onSend: () => void; onClose: () => void }) { return <section className="fixed bottom-20 right-5 z-[80] flex h-[min(520px,70vh)] w-[min(360px,calc(100vw-2rem))] flex-col rounded-2xl border border-[var(--border)] bg-[var(--card)] shadow-2xl" aria-label="AI assistant"><header className="flex items-center justify-between border-b border-[var(--border)] p-4"><div><p className="font-semibold">AI Lab Assistant</p><p className="text-xs text-[var(--muted-foreground)]">Safety-aware help</p></div><button className="touch-target" onClick={onClose} aria-label="Close assistant"><X size={17} /></button></header><div className="flex-1 space-y-3 overflow-y-auto p-4">{chat.map((message, index) => <p key={index} className={`max-w-[88%] rounded-xl px-3 py-2 text-sm ${message.role === 'user' ? 'ml-auto bg-[var(--primary)] text-white' : 'bg-[var(--background)]'}`}>{message.text}</p>)}</div><div className="flex gap-2 border-t border-[var(--border)] p-3"><input aria-label="Ask AI assistant" value={input} onChange={(event) => setInput(event.target.value)} onKeyDown={(event) => event.key === 'Enter' && onSend()} className="min-w-0 flex-1 rounded-xl border border-[var(--border)] bg-[var(--background)] px-3" placeholder="Ask assistant…" /><button className="touch-target rounded-xl bg-[var(--primary)] text-white" onClick={onSend} aria-label="Send"><Send className="mx-auto" size={16} /></button></div></section>; }
