spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/ai_laboratory}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:Sardorbek.01}

app.security.jwt-secret=${JWT_SECRET:Rh75eqEUdyBdTM8kMgLqIuAhJM9PLBb85e+R3O8Ag9A=}
app.security.access-token-ttl=${ACCESS_TOKEN_TTL:900s}
app.security.refresh-token-ttl=${REFRESH_TOKEN_TTL:30d}
app.security.refresh-cookie-same-site=${REFRESH_COOKIE_SAME_SITE:Strict}
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

app.seed.enabled=true
app.seed.admin.username=${SEED_ADMIN_USERNAME:admin}
app.seed.admin.email=${SEED_ADMIN_EMAIL:admin@ailab.local}
app.seed.admin.password=${SEED_ADMIN_PASSWORD:Admin@12345}
app.seed.user.username=${SEED_USER_USERNAME:user}
app.seed.user.email=${SEED_USER_EMAIL:user@ailab.local}
app.seed.user.password=${SEED_USER_PASSWORD:User@12345}

